#include <stdio.h>

/**
 * main - prints number 0 to 9
 *
 * Return:Always (success)
 */

int main(void)
{
	int n:

		for (n = '0'; n <= '9'; n++)
		{
			putchar(n)
		}
	putchat('\n')

		return (0);
}

